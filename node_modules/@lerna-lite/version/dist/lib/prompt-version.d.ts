import { PackageGraphNode } from '@lerna-lite/core';
export declare function makePromptVersion(resolvePrereleaseId: (prereleaseId?: string) => string | undefined): (node: PackageGraphNode) => Promise<string>;
/**
 * A predicate that prompts user to select/construct a version bump.
 * It can be run per-package (independent) or globally (fixed).
 *
 * @param {PackageGraphNode|Object} node The metadata to process
 * @property {String} currentVersion
 * @property {String} name (Only used in independent mode)
 * @property {String} prereleaseId
 */
export declare function promptVersion(currentVersion: string, name: string, prereleaseId?: string): Promise<string>;
