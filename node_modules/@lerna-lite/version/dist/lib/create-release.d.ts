import { ReleaseCommandProps, ReleaseOptions } from '@lerna-lite/core';
/**
 * @param {'github' | 'gitlab'} type
 */
export declare function createReleaseClient(type: 'github' | 'gitlab'): any;
/**
 * @param {ReturnType<typeof createReleaseClient>} client
 * @param {{ tags: string[]; releaseNotes: { name: string; notes: string; }[] }} commandProps
 * @param {{ gitRemote: string; execOpts: import('@lerna/child-process').ExecOpts }} opts
 */
export declare function createRelease(client: any, { tags, releaseNotes }: ReleaseCommandProps, { gitRemote, execOpts }: ReleaseOptions, gitDryRun?: boolean): Promise<any[]>;
